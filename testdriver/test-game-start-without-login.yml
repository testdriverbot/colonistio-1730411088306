version: 4.0.78
steps:
  - prompt: Click "Play vs Bots" without logging in.
    commands:
      - command: hover-text
        text: Play vs Bots
        description: button to play against bots
        action: click
  - prompt: Assert the game starts and allows guest play.
    commands:
      - command: assert
        expect: the game board is visible and player actions are available
  - prompt: >-
      Verify gameplay functionality.  # Use the content of the file from the
      environment
    commands:
      - command: hover-text
        text: Play vs Bots
        description: button to play against bots
        action: click
      - command: wait
        timeout: 5000
      - command: assert
        expect: the game board is visible and player actions are available
